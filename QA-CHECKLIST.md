# Magnet 验收清单（检测员 inspector 照此逐条查）

> 这是「检测员」的检查标准（写死的真相源）。每次代码改动后，检测员对照 A~F 逐项审，
> 每条给：✅通过 / 🔴阻断问题 / 🟡建议问题，附 `文件:行` + 具体改法。
> 真相源补充：内容红线/产品规则的总源在 `C:\dev\profile-attractive-master\CLAUDE.md` 与各 spec。

---

## A. 内容红线（最高优先 · 违反=上不了 App Store）
评作品不评人；不碰脸/身材/颜值/性吸引力；物化词全禁（青系/尤物/极品/白幼瘦/××天花板/行走的××等）；
零数字零排名（无分数/百分比/段位/第一/顶级）；不制造容貌焦虑；不强化性别/年龄/地域/身材刻板印象；
人人体面（没有"差"的结论，弱点只说"可以更…"的可改变方向）；朋友口吻（不油、不术语、不说教）。
**查**：所有 UI 文案、mock 数据、组件里硬编码的中文、错误/toast 提示、分享卡文案。

## B. 安全（违反=被刷/被攻击/泄密）
- 密钥/token 绝不出现在前端代码，不进 git（`.env`/`.env.local` 必须 gitignored）
- 不在 `EXPO_PUBLIC_*` 或任何打进包的常量里放 secret
- 付费不可绕：credits 真相源=**服务端**；前端**不能**本地直接加次数
- 扣费点 `analyzeBase` 必须带 `user_id`（服务端原子扣，失败自动退回）
- 购买走真实 StoreKit + 服务端验收据（verifyPurchase）+ transaction 防重放
- 报告/付费状态不能被前端伪造解锁

## C. 设计 / UI 一致性
- 颜色/字号/间距/圆角全部从 `src/constants/theme.js` 取，不自由发挥
- 这些最新 UI 必须在、且没被退回旧版：
  - 「我的」MeScreen（额度 / 最近报告 / 隐私 / 条款 / 恢复购买 / 联系）
  - 改版分享卡（首图 + 人设名 + 叙事，**无**类型标签）
  - 加减分新排版（绿色「加分方向」/ 红色「先收一收」大标题 + 白字一行一条）
  - 类型**最多选 2**（「别贪心」提示）
  - **1~3 张**图上传（IG / 朋友圈 / 小红书 提示）
  - 进度条**无**「中上/中」等级字
  - **无** demo 按钮
  - 「想要吸引的类型」命名（不是「目标画像」）

## D. 代码健壮性
- 每个 API 调用都有 `.catch` + 用户可见提示（toast/Alert），失败不白屏
- 取后端字段有空防护（`||{}` / `||[]` / 可选链），残缺数据走兜底不崩
- 导航 / 返回 / 边界 toast 正常
- 没有死代码 / 未使用的 import / 已删 action 的残留 dispatch

## E. 流程逻辑
- 12 屏导航闭环；返回不串台
- credits 流转：启动 fetchCredits 拉服务端 → 购买/扣费后 refreshCredits 同步
- demo 模式（若保留）不污染真实流程

## F. 构建铁律（别再踩 · 见项目日志 2026-06-10 傍晚）
- `react-native-iap` 锁 **v12 老架构**；**别升 v15**（Nitro 让 swift-frontend 编译器崩溃）
- `app.json` **不开** `newArchEnabled`、**不加** iap config plugin
- `eas.json` 的 ios `image` = `latest`（v12.16 的 StoreKit2 需 iOS18.4 SDK 的 `appTransactionID`）
- 记死：`expo export` 通过 **≠** 能出包；native 兼容**只能**靠 EAS build 验证

---

## 检测员查不到、必须真机肉眼确认的（每次提醒用户）
- 界面观感 / 排版是否真的好看、对齐
- 真实 Apple 支付弹窗、沙盒购买能否走通到账
- 真机性能 / 卡顿 / 闪退
- 图片上传选图、相册权限弹窗
